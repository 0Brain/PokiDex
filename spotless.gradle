apply plugin: "com.diffplug.gradle.spotless"
spotless {
  kotlin {
    target "**/*.kt"
    ktlint().userData(['indent_size': '1', 'continuation_indent_size': '1'])
    licenseHeaderFile '../spotless.license.kt'
    trimTrailingWhitespace()
    endWithNewline()
  }
}